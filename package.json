{
  "name": "cdt-inteligencia",
  "version": "1.2.4",
  "description": "Central de Tarefas do Departamento de Inteligência da Flexibase",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "node ./scripts/run-project.mjs dev",
    "dev:server": "node ./scripts/run-project.mjs dev:server",
    "build": "node ./scripts/run-project.mjs build",
    "lint": "node ./scripts/run-project.mjs lint",
    "audit:runtime": "npm audit --omit=dev",
    "audit:full": "npm audit",
    "ci": "npm run lint && npm run build && npm run audit:runtime",
    "start": "node ./scripts/run-project.mjs start",
    "install:all": "npm install && npm install --workspace=frontend && npm install --workspace=backend"
  },
  "engines": {
    "node": ">=20.19.0 <21",
    "npm": ">=10 <11"
  },
  "overrides": {
    "eslint@9.38.0": {
      "minimatch": "3.1.5"
    },
    "tinyglobby@0.2.15": {
      "picomatch": "4.0.4"
    },
    "cosmiconfig": {
      "yaml": "1.10.3"
    }
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "playwright": "^1.58.2",
    "shadcn": "^4.1.2"
  },
  "dependencies": {
    "path-to-regexp": "^0.1.13",
    "qs": "^6.14.2"
  }
}
